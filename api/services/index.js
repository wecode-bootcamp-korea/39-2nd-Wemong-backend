const authService = require('./authService');
const lectureService = require('./lectureService');
const userService = require('./userService');

module.exports = { authService, lectureService, userService };
