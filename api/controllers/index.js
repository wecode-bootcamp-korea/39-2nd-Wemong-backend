const authController = require('./authController');
const lectureController = require('./lectureController');
const userController = require('./userController');
module.exports = { authController, lectureController, userController };
